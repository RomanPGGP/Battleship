@page
@model WebApplication1.Models.Battleship
@{

    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="active">
    <h2>Deploy your fleet!</h2><br />

    <div class="wrapper">

        <p>Model.hits</p>

        @foreach (var item in Model.grid)
        {
            if (item)
            {
                <div class="boxm box1"> <a name="gridbx" value=@item asp-controller="Battleship" asp-action="PlaceShip">Bs</a> </div>
            }
            else
            {
                <div class="box box1"> <a name="gridbx" value=@item asp-controller="Battleship" asp-action="PlaceShip">Bs</a> </div>
            }
        }

                                   @*@for (int i = 0; i < 25; i++)
        {
            <div class="box box1"> <a name="gridbx" value=@i asp-controller="Battleship" asp-action="PlaceShip">Bs</a> </div>
        }*@
    <div>

</div>
